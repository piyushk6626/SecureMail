# macOS only: lets the uv-managed interpreter find Homebrew Pango/cairo at Step 9.
DYLD_FALLBACK_LIBRARY_PATH=/opt/homebrew/lib

# Step 11+ placeholders. Not required for the CLI path.
# DATABASE_URL=
# OIDC_ISSUER=
